import { SignupForm } from "./SignupForm";

// Igual ao /login: identidade genérica da plataforma, não de um
// restaurante específico (é aqui que um restaurante novo nasce).
export default function SignupPage() {
  return <SignupForm />;
}
